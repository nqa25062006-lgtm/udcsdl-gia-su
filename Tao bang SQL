CREATE TABLE IF NOT EXISTS `TaiKhoan` (
	`MaTK` VARCHAR(255) NOT NULL UNIQUE,
	`TenDangNhap` VARCHAR(255) NOT NULL,
	`MK` VARCHAR(255) NOT NULL,
	PRIMARY KEY(`MaTK`)
);


CREATE TABLE IF NOT EXISTS `NguoiDung` (
	`MaND` VARCHAR(255) NOT NULL UNIQUE,
	`HoTen` VARCHAR(255) NOT NULL,
	`SDT` VARCHAR(11) NOT NULL UNIQUE,
	`DiaChi` VARCHAR(255) NOT NULL,
	PRIMARY KEY(`MaND`)
);


CREATE TABLE IF NOT EXISTS `PhuHuynh` (
	`MaPH` VARCHAR(255) NOT NULL UNIQUE,
	`QuanHe` VARCHAR(255),
	PRIMARY KEY(`MaPH`)
);


CREATE TABLE IF NOT EXISTS `GiaSu` (
	`MaGS` VARCHAR(255) NOT NULL UNIQUE,
	`TrinhDo` VARCHAR(255) NOT NULL,
	`KinhNghiem` VARCHAR(255) NOT NULL,
	`ChungChi` VARCHAR(255),
	`BangCap` VARCHAR(255),
	PRIMARY KEY(`MaGS`)
);


CREATE TABLE IF NOT EXISTS `UngTuyen` (
	`MaUT` VARCHAR(255) NOT NULL UNIQUE,
	`NgayUT` DATE NOT NULL,
	`TTUT` VARCHAR(255) NOT NULL,
	PRIMARY KEY(`MaUT`)
);


CREATE TABLE IF NOT EXISTS `YCML` (
	`MaYCML` VARCHAR(255) NOT NULL UNIQUE,
	`Lop` INTEGER NOT NULL,
	`ThoiGianDK` VARCHAR(255) NOT NULL,
	`HocPhi` INTEGER NOT NULL,
	`YeuCauGS` VARCHAR(255) NOT NULL,
	`NgayLap` DATE NOT NULL,
	`NgayThayDoi` DATE,
	PRIMARY KEY(`MaYCML`)
);


CREATE TABLE IF NOT EXISTS `Mon` (
	`MaMon` VARCHAR(255) NOT NULL UNIQUE,
	`TenMon` VARCHAR(255) NOT NULL,
	PRIMARY KEY(`MaMon`)
);


CREATE TABLE IF NOT EXISTS `KhuVuc` (
	`MaKhuVUc` VARCHAR(255) NOT NULL UNIQUE,
	`TenKhuVuc` VARCHAR(255) NOT NULL,
	PRIMARY KEY(`MaKhuVUc`)
);


CREATE TABLE IF NOT EXISTS `LichSuTT` (
	`MaLSTT` VARCHAR(255) NOT NULL UNIQUE,
	`NgayDoiTT` DATE,
	PRIMARY KEY(`MaLSTT`)
);


CREATE TABLE IF NOT EXISTS `TTYeuCau` (
	`MaTTYC` VARCHAR(255) NOT NULL UNIQUE,
	`TenTT` VARCHAR(255) NOT NULL,
	PRIMARY KEY(`MaTTYC`)
);


CREATE TABLE IF NOT EXISTS `HopDong` (
	`MaHD` VARCHAR(255) NOT NULL UNIQUE,
	`NgayBD` DATE NOT NULL,
	`NgayKT` DATE NOT NULL,
	`PhiNhanLop` INTEGER NOT NULL,
	`NgayTao` DATE NOT NULL,
	`TrangThai` VARCHAR(255) NOT NULL,
	PRIMARY KEY(`MaHD`)
);


CREATE TABLE IF NOT EXISTS `PhatSinh` (
	`MaPhatSinh` VARCHAR(255) NOT NULL UNIQUE,
	`SoTien` INTEGER NOT NULL,
	`NgayPhatSinh` DATE NOT NULL,
	`MoTa` VARCHAR(255) NOT NULL,
	PRIMARY KEY(`MaPhatSinh`)
);


CREATE TABLE IF NOT EXISTS `LoaiPhatSinh` (
	`MaLoaiPhatSinh` VARCHAR(255) NOT NULL UNIQUE,
	`TenPhatSinh` VARCHAR(255) NOT NULL,
	`HinhThucXuLy` VARCHAR(255) NOT NULL,
	PRIMARY KEY(`MaLoaiPhatSinh`)
);


CREATE TABLE IF NOT EXISTS `HoaDon` (
	`MaHoaDon` INTEGER NOT NULL AUTO_INCREMENT UNIQUE,
	`NgayLap` DATE NOT NULL,
	`TongTien` INTEGER NOT NULL,
	`GiaNhanLop` INTEGER NOT NULL,
	`DieuChinh` INTEGER,
	PRIMARY KEY(`MaHoaDon`)
);


CREATE TABLE IF NOT EXISTS `ThanhToan` (
	`MaThanhToan` VARCHAR(255) NOT NULL UNIQUE,
	`SoTien` INTEGER NOT NULL,
	`NgayThanhToan` DATE NOT NULL,
	PRIMARY KEY(`MaThanhToan`)
);


CREATE TABLE IF NOT EXISTS `TTHoaDon` (
	`MaTTHD` VARCHAR(255) NOT NULL UNIQUE,
	`TenTT` VARCHAR(255) NOT NULL,
	PRIMARY KEY(`MaTTHD`)
);



